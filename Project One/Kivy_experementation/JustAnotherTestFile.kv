

<RootWidget>:
    orientation: 'horizontal'
    padding: '10dp'
    size_hint: 1, 1

    BoxLayout:
        orientation: 'vertical'
        CustomButton:

        CustomButton:

    BoxLayout:
        orientation: 'vertical'

        CustomButton:

        CustomButton:

<CustomButton@Button>:
    FloatLayout:
        size: (min(self.width,self.height),min(self.width,self.height))

        Button:
            size_hint: 0, 0
            size: self.size

#        FloatLayout:
#            Label:
#                id: thislabel
#                text: 'this is a test label'
#                pos: root.width / 2 - thislabel.width / 2, root.height / 2 - thislabel.height /2
#                #pos_hint: None,None
#
#            Button:
#                id: thisbutton
#                size: 200,100
#                size_hint: None, None
#        #        pos: root.width - thisbutton.width, root.height - thisbutton.height
#                text: 'button'
#    #            pos: 'right', 'top'
#    #            pos_hint: {'right':1, 'top':1}
#                pos_hint: {"right": 1, "top": 1}

# why would you wanna use the widget class..? Figure out how screen managers work.